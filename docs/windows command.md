## Windows Command
Windows testing is much easier with respect to ARM testing with QEMU.

The basic command to be issued to test directly on the Windows host is the one below, that derives from the implemented Make file:

'''
  make test_win
